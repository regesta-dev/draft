services:
  regesta:
    build: .
    environment:
      HOST: 0.0.0.0
      NODE_ENV: development
      PORT: 4321
      REGESTA_DATA_DIR: /data
    ports:
      - 4321:4321
    volumes:
      - regesta-data:/data

volumes:
  regesta-data:
